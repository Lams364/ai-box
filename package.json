{
  "name": "ai-box",
  "version": "0.1.0",
  "workspaces": [
    "log-advice-generator"
  ],
  "scripts": {
    "start":"code --extensionDevelopmentPath=${workspaceFolder}/log-advice-generator",
    "build":"docker-compose up --build"
  }
}